# Development environment defaults — committed to git.
# Override any value locally by adding it to .env.local or .env.development.local
# (both are gitignored).

NODE_ENV=development
LOG_LEVEL=debug
DB_LOGGING=false
SESSION_SECRET=local-secret-key