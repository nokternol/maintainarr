# Test environment defaults — committed to git.
# Applied when NODE_ENV=test (vitest, CI).
# .env.local is intentionally NOT loaded in test to prevent personal dev
# secrets from leaking into the test run.

NODE_ENV=test
LOG_LEVEL=error
DB_PATH=:memory:
DB_LOGGING=false
